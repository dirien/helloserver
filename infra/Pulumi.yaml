name: hello-server-eks
description: EKS infrastructure for the Hello Server Go application
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
